{
  "name": "cypress-mc-test-server",
  "version": "2.6.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:ci": "docker-compose -f docker-compose.yml -f docker-compose.ci.yml up --build --remove-orphans --exit-code-from cypress"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "15.10.0",
    "typescript": "^5.9.3"
  }
}
